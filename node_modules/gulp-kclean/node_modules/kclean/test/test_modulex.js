var kclean = require('kclean'),
    fs = require('fs');

var code = fs.readFileSync("modulex.js").toString(),
    cleanedCode = kclean.clean(code,{
         outputModule:"m/a"
    });

fs.writeFileSync("clean_modulex.js",cleanedCode);