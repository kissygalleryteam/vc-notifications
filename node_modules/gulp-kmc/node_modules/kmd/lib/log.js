var http = require('http'),
    util = require('util'),
    os = require('os'),
    pkg = require('../package.json');

var url = util.format('http://log.mmstat.com/femdata.1.2.1?u=%s&v=%s', os.hostname(), pkg.version);

module.exports = function(){
    http.get(url).on('error', function(e){});
}